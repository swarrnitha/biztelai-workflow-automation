# Optional future backend/OCR configuration.
# The current prototype runs fully in the browser and does not require secrets.

OCR_PROVIDER=openrouter
OPENROUTER_API_KEY=
OPENROUTER_MODEL=google/gemini-2.5-flash
